# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Variáveis de ambiente (chaves do Supabase)
.env
.env.local

# PDFs gerados localmente ao testar a exportação de prontuário/atendimentos —
# contêm dados de paciente (mesmo sintéticos) e não são código-fonte.
*.pdf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Ferramentas de agente de IA (skills, config local) — não fazem parte do app
.claude
.agents
.codex
skills-lock.json

# Material de referência de design (protótipos, mockups, assets brutos) — fonte
# para os ajustes, não código do app. Fica só no computador.
NovosAjustes/
IMG_*.jpeg
IMG_*.jpg
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
